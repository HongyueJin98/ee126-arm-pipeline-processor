The entire document contains 26 .vhd files. I've made some changes to my singlecyclecpu.vhd to make sure that all the debug ports are correctly linked.

There are four IMEMs that you can use to test and two repeated DMEM files. When you add all to your project, make sure that you only add one of them to run the simulation to avoid the conflicts.

Thank you!